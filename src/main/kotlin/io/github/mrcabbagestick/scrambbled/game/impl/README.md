# Game implementations

To create a concrete game implementation:
1. Create a directory in this directory (all stuff related to the game implementation should be put inside of it).
2. In the newly created directory create a class extending `GameTemplate`.
3. Add said class to [Games.kt](../../game/Games.kt) enum.